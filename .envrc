use flake

if [[ ! -f .vim/coc-settings.json ]]; then
  mkdir .vim
  echo '{}' > .vim/coc-settings.json
fi

new_coc_settings="$(cat .vim/coc-settings.json | jq ".[\"rust-analyzer.server.path\"] = \"$(which rust-analyzer)\"")"
echo "$new_coc_settings" > .vim/coc-settings.json
