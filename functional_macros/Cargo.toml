[package]
name = "functional_macros"
version = "0.0.1"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", default-features = false, features = ["full", "parsing", "visit", "proc-macro", "visit-mut", "clone-impls"] }
quote = "1.0"
